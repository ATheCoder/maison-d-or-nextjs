import MaisonFooter from '@/components/maison/MaisonFooter';

// Site footer: monogram + wordmark, the navigation column set, and the
// closing line. No props — the link set lives in the component.

export function Footer() {
  return <MaisonFooter />;
}
